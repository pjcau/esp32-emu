version: '3.8'

services:
  pcb-3d:
    build: .
    volumes:
      - ./scripts:/workspace/scripts:ro
      - ./output:/workspace/output
      - ../pcb:/workspace/pcb:ro
    environment:
      - DISPLAY=:99
    command: ["python3", "/workspace/scripts/generate_3d_model.py"]
